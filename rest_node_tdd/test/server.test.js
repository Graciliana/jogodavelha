const supertest = require("supertest");
const request = supertest("http://www.google.com");
test("Deve responder na porta 3001", () => {
  //acessar a url http://localhost:3001
  //verificar resposta foi 200
});
